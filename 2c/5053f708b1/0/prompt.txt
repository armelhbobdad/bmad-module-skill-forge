Base directory for this skill: /home/armel/Projects/OSS/bmad-module-skill-forge/.claude/skills/bmad-module-builder

# BMad Module Builder

## Overview

This skill helps you bring BMad modules to life — from the first spark of an idea to a fully scaffolded, installable module. It offers three paths:

- **Ideate Module (IM)** — A creative brainstorming session that helps you imagine what your module could be, decide on the right architecture (agent vs. workflow vs. both), and produce a detailed plan document. The plan then guides you through building each piece with the Agent Builder and Workflow Builder.
- **Create Module (CM)** — Takes an existing folder of built skills (or a single skill) and scaffolds the module infrastructure that makes it installable. For multi-skill modules, generates a dedicated `-setup` skill. For single skills, embeds self-registration directly into the skill. Supports `--headless` / `-H`.
- **Validate Module (VM)** — Checks that a module's structure is complete and correct — every skill has its capabilities registered, entries are accurate and well-crafted, and structural integrity is sound. Handles both multi-skill and standalone modules. Supports `--headless` / `-H`.

**Args:** Accepts `--headless` / `-H` for CM and VM paths, an initial description for IM, or a path to a skills folder or single SKILL.md file for CM/VM.

## On Activation

Load available config from `{project-root}/_bmad/config.yaml` and `{project-root}/_bmad/config.user.yaml` (root level and `bmb` section). If neither exists, fall back to `{project-root}/_bmad/bmb/config.yaml` (legacy per-module format). If still missing, let the user know `bmad-builder-setup` can configure the module at any time. Use sensible defaults for anything not configured.

Detect user's intent:

- **Ideate / Plan** keywords or no path argument → Load `./references/ideate-module.md`
- **Create / Scaffold** keywords, a folder path, or a path to a single SKILL.md file → Load `./references/create-module.md`
- **Validate / Check** keywords → Load `./references/validate-module.md`
- **Unclear** → Present options:
  - **Ideate Module (IM)** — "I have an idea for a module and want to brainstorm and plan it"
  - **Create Module (CM)** — "I've already built my skills and want to package them as a module"
  - **Validate Module (VM)** — "I want to check that my module's setup skill is complete and correct"

If `--headless` or `-H` is passed, route to CM with headless mode.

---

what is the best mnove?

---

<task-notification>
<task-id>wma37e4i0</task-id>
<tool-use-id>toolu_01GSWhdVoCTdEUQMxSAu9NLU</tool-use-id>
<output-file>/tmp/claude-1000/-home-armel-Projects-OSS-bmad-module-skill-forge/a3b85828-04bb-4924-b26d-a3b3e612aed8/tasks/wma37e4i0.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Investigate the SKF vs generic module-validator mismatch and recommend the best move" completed</summary>
<result>{"scouts":[{"question":"What is the provenance and mutability of the `bmad-module-builder` skill and its validator `.claude/skills/bmad-module-builder/scripts/validate-module.py` in this repo — is editing the validator (a) a durable committed change owned by this repo, (b) a local patch overwritten on reinstall, or (c) throwaway?","answer":"(b) A local patch that gets overwritten on the next BMAD install/update. The skill is an INSTALLED, upstream-vendored BMad Builder (bmb) artifact, not owned or version-controlled by this repo. `.claude/skills/bmad-module-builder/` is NOT git-tracked — `git ls-files .claude/skills/bmad-module-builder` returns empty, `git ls-files .claude` counts 0, and `git ls-files --error-unmatch` on the validator fails (\"did not match any file(s) known to git\"). The entire `.claude/` tree is gitignored by the bare `.claude` entry at `.gitignore:54` (confirmed by `git check-ignore -v`). The file exists on disk (14072 bytes, mtime Jul 13 01:17) but has zero git history (`git log --all -- .claude/skills/bmad-module-builder` is empty), so it has never been committed. It is an installer-generated artifact: `_bmad/bmb/config.yaml` is headed \"Generated by BMAD installer / Version: 6.10.0 / Date: 2026-07-12T21:17:48.061Z\" (matching the file mtimes), `_bmad/_config/manifest.yaml:20` lists `- name: bmb` as an installed module, and `_bmad` itself is gitignored. The skill has no source under `src/` (this repo authors only `skf-*` skills, and marketplace.json publishes only `./src/skf-*`), confirming `bmad-module-builder` is vendored from upstream BMad, not hand-maintained here. It is NOT (a) durable/committed (untracked + gitignored + no history) and NOT (c) session-throwaway (it persists on disk across sessions) — it is (b): edits survive until the next `bmad`/BMB installer run regenerates the bmb module and clobbers them. The canonical place to fix this validator is upstream in the BMad Method / BMad Builder source, not this repo.","confidence":"high","evidence":["git ls-files .claude/skills/bmad-module-builder → (empty output, no tracked files)","git ls-files .claude | wc -l → 0","git ls-files --error-unmatch .claude/skills/bmad-module-builder/scripts/validate-module.py → error: pathspec ... did not match any file(s) known to git (exit 1)","git check-ignore -v .claude/skills/.../validate-module.py → .gitignore:54:.claude\t.claude/skills/bmad-module-builder/scripts/validate-module.py (exit 0)",".gitignore line 54: bare `.claude` (also line 39 `.claude/settings.local.json`, line 69 `.claude/.story-automator-active`); `_bmad` and `_bmad-output` also gitignored","ls -la .claude/skills/.../validate-module.py → -rw-rw-r-- 14072 Jul 13 01:17 (exists on disk)","git log --all --oneline -- .claude/skills/bmad-module-builder → (empty: never committed)","_bmad/bmb/config.yaml header: '# Generated by BMAD installer / # Version: 6.10.0 / # Date: 2026-07-12T21:17:48.061Z'","_bmad/_config/manifest.yaml:20 → '  - name: bmb'","find src -iname '*module-builder*' → (empty); src/ contains only skf-* skills; .claude-plugin/marketplace.json publishes only ./src/skf-* skills",".claude/skills/ contains the full installed BMad skill set (bmad-advanced-elicitation ... bmad-module-builder ...), i.e. compiled/installed artifacts not repo source"],"implications":"Do not edit `.claude/skills/bmad-module-builder/scripts/validate-module.py` expecting the change to be committed or reviewed — it is gitignored and invisible to git, and the BMAD installer (currently v6.10.0) will overwrite the whole bmb module on the next install/update, discarding any local edit. A fix here is only useful as a temporary local experiment. To make a durable fix to this validator, contribute it upstream to the BMad Method / BMad Builder source, then reinstall. This repo owns only the `skf-*` skills under `src/` (published via marketplace.json); the `bmad-*` skills in `.claude/skills/` are installed dependencies."},{"question":"Is SKF's module-manifest layout (root-level module.yaml/module-help.csv with after/before columns) conformant with the real BMad convention or drift, and is the scaffolder validator merely checking a pre-install source layout SKF doesn't use?","answer":"The verdict SPLITS across two independent axes. AXIS 1 (FILE LOCATION): SKF is CONFORMANT with the installed runtime, and the validator checks a pre-install SOURCE layout SKF legitimately does not use. All three installed BMad modules keep module-help.csv at MODULE ROOT (not in &lt;skill&gt;/assets/); SKF ships src/module-help.csv + src/module.yaml at root and its installer verbatim-copies them into _bmad/skf/ root. The validator's assets/module-help.csv + assets/module.yaml expectation is the setup-skill SOURCE-scaffold form, so its location-based finding is a false positive against SKF's runtime-correct root placement. AXIS 2 (COLUMN NAMES): SKF is genuinely NON-CONFORMANT / drift. preceded-by/followed-by is the REAL ecosystem column convention, used by BOTH the installed runtime (bmm/core/bmb) AND the scaffolder source template. SKF's after/before matches NEITHER. The task's premise that preceded-by/followed-by might be only the scaffolder SOURCE form is FALSE. Bottom line: SKF conforms on placement but has a real, functional column-name drift (after/before) that reaches the installed CSV unchanged.","confidence":"high","evidence":["Installed CSVs all at MODULE ROOT: `find _bmad -name module-help.csv` returned exactly _bmad/bmm/module-help.csv, _bmad/core/module-help.csv, _bmad/bmb/module-help.csv (none in an assets/ subfolder); `find _bmad -name module.yaml` returned NOTHING","Installed header, identical across bmm/core/bmb (first line): module,skill,display-name,menu-code,description,action,args,phase,preceded-by,followed-by,required,output-location,outputs","SKF src/module-help.csv:1 header uses after/before: module,skill,display-name,menu-code,description,action,args,phase,after,before,required,output-location,outputs -- and src/module.yaml + src/module-help.csv both sit at src/ root (src/assets/module.yaml does not exist)","Scaffolder SOURCE template emits preceded-by/followed-by at .claude/skills/bmad-module-builder/assets/setup-skill-template/assets/module-help.csv (header: module,skill,display-name,menu-code,description,action,args,phase,preceded-by,followed-by,required,output-location,outputs); its module.yaml lives under .../setup-skill-template/assets/module.yaml","validate-module.py expects assets/ layout AND preceded-by/followed-by: lines 178-179 read setup_dir/assets/module.yaml and setup_dir/assets/module-help.csv; line 31 REQUIRED_COLUMNS include 'preceded-by','followed-by'; line 281 iterates for field in ('preceded-by','followed-by')","SKF installer does verbatim copy, no rename/relocation: tools/cli/lib/installer.js:172-178 loops ['module.yaml','module-help.csv'] and fs.copy(src, path.join(skfDir, file)) into _bmad/skf/ root, so after/before columns are preserved as-is into the installed CSV"],"implications":"Two separate follow-ups with opposite dispositions. (1) The validator's assets/-location expectation is a SOURCE-scaffold assumption that does not describe the installed runtime, so failing SKF on module.yaml/module-help.csv placement is a validator false positive -- SKF's root placement matches bmm/core/bmb exactly. Do NOT 'fix' SKF by moving these files into assets/. (2) SKF's after/before columns ARE genuine drift: the real convention (installed runtime AND scaffolder source) is preceded-by/followed-by, and the installer copies SKF's CSV unchanged, so any consumer reading preceded-by/followed-by would see SKF's ordering columns as absent/empty. Renaming SKF's columns to preceded-by/followed-by is the conformance fix; the validator is correct on the column axis even though it is wrong on the location axis."},{"question":"What consumes SKF's src/module.yaml and src/module-help.csv, and are the after/before CSV columns load-bearing? (a) Does anything parse after/before to build an ordered 
... (truncated 24337 chars, full result in /tmp/claude-1000/-home-armel-Projects-OSS-bmad-module-skill-forge/a3b85828-04bb-4924-b26d-a3b3e612aed8/tasks/wma37e4i0.output)</result>
<diagnostics>Per-agent results: /home/armel/.claude/projects/-home-armel-Projects-OSS-bmad-module-skill-forge/9b098287-9867-4b79-a427-a2e16ba864c8/subagents/workflows/wf_5a3840f5-aad/journal.jsonl — one {"type":"result",...} line per completed agent with its full return value.
If the result above is empty or unexpected, Read this file BEFORE diagnosing — do not assume agents returned non-empty results.
To re-run with edited post-processing: Workflow({scriptPath: '/home/armel/.claude/projects/-home-armel-Projects-OSS-bmad-module-skill-forge/9b098287-9867-4b79-a427-a2e16ba864c8/workflows/scripts/skf-validator-fit-decision-wf_5a3840f5-aad.js', resumeFromRunId: 'wf_5a3840f5-aad'}) — agents whose (prompt, opts) are unchanged replay from cache.</diagnostics>
<usage><agent_count>6</agent_count><agents_done>6</agents_done><agents_error>0</agents_error><agents_skipped>0</agents_skipped><agents_empty_result>0</agents_empty_result><subagent_tokens>330365</subagent_tokens><tool_uses>80</tool_uses><duration_ms>629367</duration_ms></usage>
</task-notification>

---

go on

---

go on