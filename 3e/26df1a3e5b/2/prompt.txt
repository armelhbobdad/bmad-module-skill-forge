ultracode  — Address the four #427 follow-up issues (#430, #431, #432, #433) on a
  single new branch off main, as a sequence of small, test-first commits. They were
  all spun off from the language-reference work merged in PR #429 — read the
  `project_issue_427_language_reference` memory first for what shipped and why.

  The four:
  - #432  Carry per-corpus provenance (source / detected_via) through the doc_urls
          schema + brief writer. Likely foundational (the others touch the same
          doc_urls surface). Treat it as a lockstep change — find EVERY schema,
          validator, writer, and test that touches doc_urls before editing.
  - #431  Suppress non-corpus README noise in language-reference doc_urls (drop
          same-host /whatsnew/, /contribute/, /wiki/, non-primary-locale dupes);
          also add the vendored-grammar false-positive denylist if warranted.
  - #430  Rank prose ABOVE code in create-skill assembly for whole-language
          language-reference skills. The deepest one: today T1(code) outranks
          T3(docs), but for a compiler-repo-plus-Book skill the prose IS the point.
          Assembly/enrich side — high regression risk for every existing
          language-reference skill.
  - #433  Extend shape detection to more ecosystems (Maven pom.xml, Gradle
          build.gradle*, Swift Package.swift). Independent; mirror the go.mod
          addition in skf-shape-detect.py + the auto-scope filter.

  How I want you to work (same rigor as the #427 work):
  - Design before code. Use party mode and/or design workflows to converge and to
    red-team the risky parts — especially #430 (reordering assembly tiers can
    regress every existing language-reference skill: prove it doesn't) and #432
    (map the full doc_urls lockstep surface). Bring the design + commit plan back
    to me BEFORE implementing the risky pieces.
  - Decide and justify the commit order yourself, accounting for the interactions
    (#432 provenance likely first so #431 can drop by source; #430 and #431 share
    the doc_urls area; #433 is parallel).
  - Empirically validate against the real scripts / a real forge where it matters,
    not just unit tests. Keep package.json test:python enumerating any new test
    file. Commits stay feature-focused with no internal references; close each
    issue with `Fixes #NNN`.
  - One branch off main, multiple commits. Do NOT push or open a PR until I
    approve — prepare it locally and show me.

  Start by reading the four issues and the memory, then propose the design and the
  ordered commit plan.

---

<task-notification>
<task-id>w05ynogzp</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-armel-Projects-OSS-bmad-module-skill-forge/35600871-ad6f-4c41-8071-82c59eec462f/tasks/w05ynogzp.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Design + adversarially red-team the four #427 follow-up issues (#430/#431/#432/#433) before implementation" completed</summary>
<result>[{"issue":"432","title":"Carry per-corpus provenance (source/detected_via) through doc_urls schema + brief writer","risk":"high","design":{"issue":"#432 — Carry per-corpus provenance (a coarse `source` enum) through the doc_urls schema and the brief writer, so downstream #431 (noise-suppression filter) and #430 (assembly tier re-ordering) can distinguish registry-guaranteed corpora from opportunistically-detected docs.","approach_summary":"Add a single optional coarse `source` enum to each `doc_urls` item — values {language-registry, readme-detection, homepage, pages-api, docs-folder} — chosen over re-threading the fine `detected_via` because both downstream consumers (#430, #431) need only the registry-vs-detected class, and `detected_via` is a detect-docs-internal vocabulary that never reached the brief schema. The change is additive: the schema's doc_urls items have no `additionalProperties:false` (verified skill-brief.v1.json:49-55), so extra keys already validate. The single behavioral drop point is `assemble_brief` (skf-write-skill-brief.py:438-440), which rebuilds each entry as `{url,label}`; it is widened to copy `source` when present. The two producer/stamp prose sites (analyze-source §6b/§8 corpora seed → `source: language-registry`; brief-skill §3 README-detection map → `source` derived from `detected_via`) emit the field, and `skf-language-corpora.py` is extended to emit `source: language-registry`. Skills/briefs that carry no `source` are byte-identical (the writer only emits the key when present, mirroring the existing label/doc_urls null-drop discipline), so no false drift for non-language-reference shapes.","files_to_change":[{"path":"/home/armel/Projects/OSS/bmad-module-skill-forge/src/shared/scripts/schemas/skill-brief.v1.json","change":"In the doc_urls items.properties block (lines 52-55) add a third optional property: `\"source\": { \"type\": \"string\", \"enum\": [\"language-registry\", \"readme-detection\", \"homepage\", \"pages-api\", \"docs-folder\"] }`. Do NOT add `required` for it and do NOT add `additionalProperties:false` (items currently has neither, verified lines 49-56). 'url' stays the only required key.","why":"Foundational schema admits the new optional provenance field with a closed enum so #431/#430 can branch on a validated value. Additive: Draft202012Validator (skf-validate-brief-schema.py:276) accepts existing briefs unchanged."},{"path":"/home/armel/Projects/OSS/bmad-module-skill-forge/src/shared/scripts/skf-write-skill-brief.py","change":"At the doc_urls emission (lines 436-440) replace the comprehension that rebuilds `{\"url\": e[\"url\"], \"label\": e.get(\"label\", \"\")}` with one that conditionally carries `source`: build the base `{url,label}` then `if e.get(\"source\"): entry[\"source\"] = e[\"source\"]`. This is the sole drop point: flat_to_nested (lines 540-545) already passes unknown keys through, and validate_context (lines 234-242) only checks url. Optionally add light validation in validate_context: when an entry has `source`, warn (not _die) if not in the enum, mirroring the soft-validation ethos.","why":"This rebuild is the only place provenance is discarded today. Conditional emission keeps briefs without `source` byte-identical (no false drift), matching the existing source_authority/scripts_intent non-default-only emission discipline (lines 442-454)."},{"path":"/home/armel/Projects/OSS/bmad-module-skill-forge/src/shared/scripts/skf-language-corpora.py","change":"In corpora_for() (lines 64-68) change the appended dict from `{\"url\": e[\"url\"], \"label\": e.get(\"label\", \"\")}` to also include `\"source\": \"language-registry\"`. Update the module docstring (lines 16-17, 22) which currently says output is `{url, label}` to `{url, label, source}`.","why":"Registry corpora are the registry-guaranteed class; stamping `source: language-registry` at the source is cleaner than asking the §6b prose to inject it per-seed. Directly serves #431's 'when a registry corpus exists for a doc host' test."},{"path":"/home/armel/Projects/OSS/bmad-module-skill-forge/src/skf-analyze-source/references/step-auto-scope.md","change":"§6b (line 444): change 'parse the `[{url, label}]` array' to `[{url, label, source}]`. §8 corpora seed block (lines 600-603): change the YAML template `- { url: '{seed.url}', label: '{seed.label}' }` to include `source: 'language-registry'` (or `source: '{seed.source}'` since the script now emits it).","why":"The producer prose must reflect that seeds now carry provenance into the brief doc_urls, keeping the runtime instruction consistent with the script output."},{"path":"/home/armel/Projects/OSS/bmad-module-skill-forge/src/skf-brief-skill/references/step-auto-brief.md","change":"§3 (lines 88-101): add a `source` field to the constructed doc_urls entry, derived from `detected_via`: homepageUrl→homepage, readme_link→readme-detection, pages_api→pages-api, docs_folder→docs-folder. In the merge rule (line 101) clarify that on a normalized-URL dedup collision the existing (corpora-seeded, source: language-registry) entry wins and retains its `source`.","why":"This is the brief-side stamp that currently drops detected_via entirely; mapping it to the coarse `source` is what makes README-detected docs distinguishable from registry corpora at the brief boundary, the exact distinction #431 filters on."},{"path":"/home/armel/Projects/OSS/bmad-module-skill-forge/src/skf-brief-skill/assets/skill-brief-schema.md","change":"Optional Fields table doc_urls row (line 22): change constraint cell `{url, label} objects` to `{url, label, source} objects` and note `source` is optional with enum {language-registry, readme-detection, homepage, pages-api, docs-folder}. Optionally extend the commented YAML template (lines 197-200) with a `source:` example line.","why":"Prose schema mirror must match the JSON schema or the drift validators / human readers diverge. Mirror-only doc change."},{"path":"/home/armel/Projects/OSS/bmad-module-skill-forge/src/skf-analyze-source/assets/skill-brief-schema.md","change":"Optional Fields table doc_urls row (line 26): same edit as the brief-skill mirror, `{url, label, source} objects`, document the optional `source` enum.","why":"Second prose schema mirror; analyze-source generates briefs so its contract doc must list the field it now emits via §6b/§8."},{"path":"/home/armel/Projects/OSS/bmad-module-skill-forge/test/test-skf-language-corpora.py","change":"Update test_output_is_brief_contract_shape_only (lines 43-50): the `assert set(entry.keys()) == {\"url\", \"label\"}` must become `== {\"url\", \"label\", \"source\"}` and assert `entry[\"source\"] == \"language-registry\"`. Update the docstring (lines 44-45) which currently says the writer would discard content_type/detected_via.","why":"This test pins the corpora output shape with `==` (exact equality), it WILL break when the script adds `source`. Must be updated in lockstep."},{"path":"/home/armel/Projects/OSS/bmad-module-skill-forge/test/test-skf-write-skill-brief.py","change":"Add a new test asserting assemble_brief preserves `source` when present (ctx doc_urls entry {url, label, source: 'language-registry'} round-trips with source intact) and a test that an entry WITHOUT source still emits exactly {url,label} (guard against accidental key injection). The existing test_doc_urls_emitted_with_label_default (lines 250-258) which uses `==` on no-source entries must still pass unchanged, verify it does (it will, since conditional emission adds nothing when source absent).","why":"Locks the new conditional-emission behavior and the no-false-drift guarantee for the no-source path."},{"path":"/home/armel/Projects/OSS/bmad-module-skill-forge/test/test-skf-validate-brief-schema.py","change":"Add a test that a brief with a doc_urls entry carrying a valid `source` enum value validates (valid:true), and a test that an out-of-enum `source` value is rejected by the schema enum.","why":"Confirms the additive schema enum is enforced a
... (truncated 96363 chars, full result in /tmp/claude-1000/-home-armel-Projects-OSS-bmad-module-skill-forge/35600871-ad6f-4c41-8071-82c59eec462f/tasks/w05ynogzp.output)</result>
<usage><agent_count>11</agent_count><subagent_tokens>793681</subagent_tokens><tool_uses>223</tool_uses><duration_ms>434517</duration_ms></usage>
</task-notification>